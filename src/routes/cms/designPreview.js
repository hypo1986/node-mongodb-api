const setRouter = require('./setRouter')
const designPreviewCtrl = require('../../controllers/cms/designPreview')
const router = setRouter(designPreviewCtrl)
// extend router

module.exports = router
